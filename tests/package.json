{
  "name": "expense-bookkeeping-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Dev-only property/unit test harness for the expense-bookkeeping module. NOT shipped with the PWA.",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "fast-check": "^3.23.1"
  }
}
